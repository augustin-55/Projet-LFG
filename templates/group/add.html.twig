
{{ form_start(form, {'attr': {'class': 'formulaire-ajout-groupe', 'action': path('add_groupe')}}) }}

    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Créer</button>

{{ form_end(form)}}